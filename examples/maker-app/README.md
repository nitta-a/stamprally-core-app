# Maker app

Use `RallyEditor` from `@stamprally/admin-ui` to edit the complete rally
configuration, including localized copy, spot metadata, prerequisites, reward
stock, claim limits, staff passcodes, and release conditions. Export the public
configuration with `stripSensitiveConfig` before publishing it to participants.
